---
import Navbar from '../components/navbar.astro';
import Footer from '../components/Footer.astro';
---

<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/svg+xml" href="/icon.png" />
    <title>Página no encontrada - Ivan FX</title>
    <style>
        /* Estilos para el texto 404 */
        h1 {
            font-size: 10rem;
            font-weight: bold;
            background: linear-gradient(90deg, #00aaff, #bae4ff 50%, #00aaff); /* Ajuste del degradado */
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin: 0;
            display: inline-block;
        }
        body {
            margin: 0;
            padding: 0;
            background-color: #121212; /* Fondo oscuro */
        }
        .text-center {
            text-align: center;
            margin-top: 10vh; /* Margen superior para centrar el contenido verticalmente */
        }
        p {
            color: #bbbbbb; /* Color claro para el texto de subtítulo */
            font-size: 1.5rem;
        }
        .texto-404 {
            text-align: center;
        }
        .btn {
            transition: transform 0.3s ease; /* Transición suave para el efecto de zoom */
        }
        .btn:hover {
            transform: scale(1.05); /* Efecto de zoom al pasar el mouse */
        }

        /* Asegurar que los cards estén centrados en móviles */
        @media (max-width: 576px) {
            .col-12 {
                display: flex;
                justify-content: center; /* Centrar los cards en pantallas móviles */
             }
        }

        .btn {
            transition: transform 0.3s ease; /* Transición suave para el efecto de zoom */
        }
        .btn:hover {
            transform: scale(1.05) rotate(-2deg); /* Efecto de zoom al pasar el mouse */
         }
    </style>
</head>
<body>
    <Navbar/>
    <br><br><br><br>
    <div class="texto-404 mt-5">
        <h1>404</h1>
        <p>Lo sentimos, la página que buscas no fue encontrada.</p>
        <a class="btn btn-primary" href="/">Ir al Inicio</a>
    </div>
    <br><br><br><br><br><br><br><br><br>
    <Footer/>
</body>
</html>
